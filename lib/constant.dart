import 'package:flutter/material.dart';

const pcolor = Color(0xff100B20);
const rTranstionDuration = Duration(milliseconds: 250);
const rFeaturedBox = 'featured_box';
const rNewestBox = 'newest_box';
